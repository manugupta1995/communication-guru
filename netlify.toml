# One-command deploy to Netlify (free static hosting).
#   npx netlify deploy --prod
[build]
  command = "npm run build"
  publish = "dist"

# SPA: serve index.html for all routes.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
